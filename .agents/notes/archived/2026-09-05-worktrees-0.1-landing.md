# Worktrees 0.1.0 is agent-resolved landing, not shuttle

- date: 2026-09-05
- status: archived
- scope: docs/ideas/dsh-next-worktrees-0.1.md

Next milestone for packages/dsh-next-worktrees, recorded at
docs/ideas/dsh-next-worktrees-0.1.md. Isolate, nested sidebar, guarded
merge, and the abandoned-worktree sweeper already shipped; the remaining
pain is a conflict dumping the user to a copyable `git merge` command.

Direction: merge the primary branch into the worktree (row action +
conflict-modal CTA), let the bound session's agent resolve, then the
existing Merge is a fast-forward. Then un-private. Foreground/Return
(old M2) is parked — Cursor's apply-worktree is an IDE gesture, and
Merge already replaced that landing.

Status pointers added on docs/ideas/dsh-next-worktrees.md and
docs/ideas/dsh-next-worktrees-sidebar-ux.md. Implemented in
packages/dsh-next-worktrees; see the implemented note of the same date.
