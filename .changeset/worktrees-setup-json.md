---
"@dsh-next/dsh-next-worktrees": minor
---

After creating a worktree, run optional setup commands from `.worktrees.json` at the repo root (or `.dsh/worktrees.json` as a local override). A failed command cancels create.
