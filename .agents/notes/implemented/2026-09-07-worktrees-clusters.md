# Worktree clusters

- date: 2026-09-07
- status: implemented
- scope: packages/dsh-next-worktrees, packages/dsh-next-skills, packages/dsh-next-cc-plugins

Nested named cluster under the harbor: worktree workspaces stay real
groups, `deriveGroups` output is nested, git lives on the folder menu,
folder `+` starts extra chats, name modal sets the display title, skills
and cc-plugins hide `/.dsh/worktrees/` rows and match cwd to the harbor
basename.

Owning doc: `docs/ideas/dsh-next-worktrees-clusters.md`.
