---
"@dsh-next/dsh-next-worktrees": minor
---

When a setup command such as `pnpm install` fails, the worktree and session stay. The error shows the command output so you can finish setup yourself or delete the worktree.
