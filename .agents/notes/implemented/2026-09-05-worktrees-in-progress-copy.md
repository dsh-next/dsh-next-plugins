# Worktrees in-flight modal: Merge in progress

- date: 2026-09-05
- status: implemented
- scope: packages/dsh-next-worktrees

The in-flight Update modal (MERGE_HEAD) is titled Merge in progress,
not Update from `{branch}`. The primary is Resolve in this session
(same key as the would-conflict confirm). Abort merge stays the danger
control on the left. Preflight titles and the clean Update confirm are
unchanged.
