/**
 * Cordis service key this plugin provides for optional host-to-host
 * collaboration (dsh-next-reset reclaim). Consumers look the key up
 * through `ctx.get` and match a structural face — they must not import
 * this package.
 */
export const WORKTREES_SERVICE_KEY = 'dsh-next-worktrees'
